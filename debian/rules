#!/usr/bin/make -f

makebuilddir/liboptimization2-sharp::
	./autogen.sh

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

DEB_MAKE_INSTALL_TARGET = install DESTDIR=$(DEB_DESTDIR)
